[package]
name = "advance-cost-tracker"
version.workspace = true
repository.workspace = true
edition.workspace = true
license.workspace = true
description = "MODULE-019 CostTracker — CONTRACT-181 first declaration + first impl. Subscribes to llm.response events and aggregates by run_id and (run_id, iteration)."

publish = false

[dependencies]
advance-shared-types = { path = "../shared-types" }
serde_json.workspace = true

[lints]
workspace = true
